# Real secrets/config derived from .env.example — never commit.
.env

# Generated by scripts/seed.ts: issuer/client_id/JWKS per app. Client IDs for
# public PKCE clients aren't secret, but this file is a regenerable local
# artifact tied to one specific bootstrap of the instance, so it stays out
# of git like a build output.
generated-client-env.md
